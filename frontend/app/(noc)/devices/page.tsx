"use client";

import { FormEvent, useEffect, useMemo, useRef, useState } from "react";
import Link from "next/link";
import { apiFetch, ApiError } from "../../../lib/api";
import { groupSections, GroupSectionRows } from "../../../lib/device-groups";
import { Activity, CheckCircle2, Copy, Loader2, Network, Pause, Play, Plus, RefreshCw, Router, Search, Server, ShieldCheck, Wifi, X, Zap } from "lucide-react";

type Device={id:string;name:string;address:string;deviceType:string;vendor?:string;serialNumber?:string;enabled:boolean;snmpEnabled:boolean;snmpVersion:string;snmpPort:number;snmpConfigured:boolean};
type TestResult={reachable:boolean;systemName?:string;sysDescr?:string;interfaceCount?:number;error?:string};
type ScanFound={address:string;systemName?:string;sysDescr?:string;deviceType:string;vendor?:string};
type ScanJob={id:string;cidr:string;status:string;total:number;scanned:number;results:ScanFound[];error?:string};
type DeviceGroup={id:number;name:string;sortOrder:number};
type GroupMember={groupId:number;subjectType:string;subjectId:string;sortOrder:number};
const ORG="tenant-1";
const input="mt-1 w-full rounded-lg border border-slate-700 bg-slate-950 px-3 py-2.5 text-sm text-slate-100 outline-none transition focus:border-cyan-500 focus:ring-1 focus:ring-cyan-500/30 placeholder:text-slate-600";
const card="rounded-xl border border-slate-800 bg-slate-900/80 shadow-[0_10px_35px_rgba(0,0,0,.14)]";

export default function DevicesPage(){
 const [devices,setDevices]=useState<Device[]>([]),[loading,setLoading]=useState(true),[saving,setSaving]=useState(false),[testing,setTesting]=useState(false),[message,setMessage]=useState(""),[result,setResult]=useState<TestResult|null>(null),[selected,setSelected]=useState<Device|null>(null);
 const [scanJob,setScanJob]=useState<ScanJob|null>(null),[scanning,setScanning]=useState(false),[scanError,setScanError]=useState(""),[selectedAddrs,setSelectedAddrs]=useState<Set<string>>(new Set()),[importing,setImporting]=useState(false);
 const [groups,setGroups]=useState<DeviceGroup[]>([]),[memberOf,setMemberOf]=useState<Record<string,number>>({}),[collapsed,setCollapsed]=useState<Set<string>>(new Set());
 const [checked,setChecked]=useState<Set<string>>(new Set()),[bulkBusy,setBulkBusy]=useState(false),[showAdd,setShowAdd]=useState(false),[showTest,setShowTest]=useState(false),[showDiscovery,setShowDiscovery]=useState(false);
 const addFormRef=useRef<HTMLFormElement>(null);
 async function load(){setLoading(true);try{setDevices(await apiFetch<Device[]>(`/devices?organizationId=${ORG}`))}catch(e){setMessage(e instanceof ApiError?e.message:"Unable to load devices.")}finally{setLoading(false)}}
 useEffect(()=>{load()},[]);
 async function loadGroups(){try{const[gs,ms]=await Promise.all([apiFetch<DeviceGroup[]>(`/device-groups?tenantId=${ORG}`),apiFetch<GroupMember[]>("/device-groups/members")]);setGroups(gs);const map:Record<string,number>={};ms.filter(m=>m.subjectType==="device").forEach(m=>{map[m.subjectId]=m.groupId});setMemberOf(map)}catch{/* optional overlay */}}
 useEffect(()=>{loadGroups()},[]);
 async function assignGroup(d:Device,groupId:number|null){try{await apiFetch(`/device-group-assignments/device/${d.id}`,{method:"PUT",body:JSON.stringify({groupId,sortOrder:0})});await loadGroups()}catch(err){setMessage(err instanceof ApiError?err.message:"Failed to update device group.")}}
 function toggleCollapsed(key:string){setCollapsed(prev=>{const n=new Set(prev);n.has(key)?n.delete(key):n.add(key);return n})}
 function toggleChecked(id:string){setChecked(prev=>{const n=new Set(prev);n.has(id)?n.delete(id):n.add(id);return n})}
 async function bulkPause(enabled:boolean){if(!checked.size)return;setBulkBusy(true);try{await apiFetch("/devices/pause-bulk",{method:"PUT",body:JSON.stringify({ids:Array.from(checked),enabled})});setMessage(`${checked.size} device(s) ${enabled?"resumed":"paused"}.`);setChecked(new Set());await load()}catch(err){setMessage(err instanceof ApiError?err.message:"Failed to update selected devices.")}finally{setBulkBusy(false)}}
 async function togglePause(d:Device){try{await apiFetch(`/devices/${d.id}/pause`,{method:"PUT",body:JSON.stringify({enabled:!d.enabled})});setMessage(`${d.name} ${d.enabled?"paused":"resumed"}.`);await load()}catch(err){setMessage(err instanceof ApiError?err.message:"Failed to update device state.")}}
 function cloneDevice(d:Device){setShowAdd(true);setTimeout(()=>{const f=addFormRef.current;if(!f)return;(f.elements.namedItem("name") as HTMLInputElement).value=`${d.name} (Clone)`;(f.elements.namedItem("address") as HTMLInputElement).value=d.address;(f.elements.namedItem("deviceType") as HTMLSelectElement).value=d.deviceType;(f.elements.namedItem("vendor") as HTMLInputElement).value=d.vendor||"";},0)}
 function makePayload(data:FormData){const version=String(data.get("version")||"2c");return {organizationId:ORG,name:data.get("name"),address:data.get("address"),deviceType:data.get("deviceType"),vendor:data.get("vendor"),serialNumber:data.get("serialNumber"),snmpPort:Number(data.get("snmpPort")||161),timeoutMs:Number(data.get("timeoutMs")||3000),snmp:{version,community:data.get("community"),username:data.get("username"),authProto:data.get("authProto"),authPass:data.get("authPass"),privProto:data.get("privProto"),privPass:data.get("privPass")}}}
 async function add(e:FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);try{const d=await apiFetch<Device>("/devices",{method:"POST",body:JSON.stringify(makePayload(new FormData(e.currentTarget)))});setMessage(`✓ ${d.name} registered${d.snmpEnabled?" with SNMP enabled":""}.`);e.currentTarget.reset();setShowAdd(false);await load()}catch(err){setMessage(err instanceof ApiError?err.message:"Failed to register device.")}finally{setSaving(false)}}
 async function test(e:FormEvent<HTMLFormElement>){e.preventDefault();setTesting(true);setResult(null);try{setResult(await apiFetch<TestResult>("/devices/test",{method:"POST",body:JSON.stringify(makePayload(new FormData(e.currentTarget)))}))}catch(err){setResult({reachable:false,error:err instanceof ApiError?err.message:"SNMP test failed."})}finally{setTesting(false)}}
 async function saveSNMP(e:FormEvent<HTMLFormElement>){e.preventDefault();if(!selected)return;setSaving(true);try{const d=new FormData(e.currentTarget);await apiFetch(`/devices/${selected.id}/snmp`,{method:"PUT",body:JSON.stringify({enabled:d.get("enabled")==="on",version:d.get("version"),community:d.get("community"),username:d.get("username"),authProto:d.get("authProto"),authPass:d.get("authPass"),privProto:d.get("privProto"),privPass:d.get("privPass"),port:Number(d.get("port")||161),timeoutMs:Number(d.get("timeoutMs")||3000)})});setMessage(`✓ SNMP configuration saved for ${selected.name}.`);setSelected(null);await load()}catch(err){setMessage(err instanceof ApiError?err.message:"Failed to save SNMP configuration.")}finally{setSaving(false)}}
 async function startScan(e:FormEvent<HTMLFormElement>){e.preventDefault();setScanning(true);setScanError("");setScanJob(null);setSelectedAddrs(new Set());try{const d=new FormData(e.currentTarget);setScanJob(await apiFetch<ScanJob>("/discovery/scan",{method:"POST",body:JSON.stringify({cidr:d.get("cidr"),version:d.get("scanVersion")||"2c",community:d.get("scanCommunity")||"public",port:Number(d.get("scanPort")||161),timeoutMs:Number(d.get("scanTimeoutMs")||1500)})}))}catch(err){setScanError(err instanceof ApiError?err.message:"Failed to start subnet scan.")}finally{setScanning(false)}}
 useEffect(()=>{if(!scanJob||scanJob.status!=="running")return;const id=scanJob.id;const t=window.setInterval(async()=>{try{setScanJob(await apiFetch<ScanJob>(`/discovery/scan/${id}`))}catch{/* transient */}},1500);return()=>window.clearInterval(t)},[scanJob?.id,scanJob?.status]);
 async function importSelected(){if(!scanJob||!selectedAddrs.size)return;setImporting(true);try{const r=await apiFetch<{created:Device[];failed?:string[]}>("/discovery/import",{method:"POST",body:JSON.stringify({jobId:scanJob.id,organizationId:ORG,addresses:Array.from(selectedAddrs)})});setMessage(`Imported ${r.created.length} device(s) from discovery${r.failed?.length?` · ${r.failed.length} failed`:""}.`);setScanJob(p=>p?{...p,results:p.results.filter(x=>!selectedAddrs.has(x.address))}:p);setSelectedAddrs(new Set());await load()}catch(err){setScanError(err instanceof ApiError?err.message:"Failed to import devices.")}finally{setImporting(false)}}
 const stats=useMemo(()=>({total:devices.length,monitoring:devices.filter(d=>d.enabled).length,snmp:devices.filter(d=>d.snmpConfigured).length,attention:devices.filter(d=>!d.snmpConfigured).length}),[devices]);
 const groupsView=groupSections(devices,groups,memberOf);
 return <main className="mx-auto max-w-[1500px] px-4 py-6 sm:px-6 lg:px-8">
  <header className="mb-6 flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between"><div><div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[.18em] text-cyan-400"><Network size={14}/> Infrastructure / Devices</div><h1 className="text-3xl font-bold tracking-tight text-white">Network device management</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">One professional workspace to register infrastructure, configure SNMP, verify connectivity and move devices into active monitoring.</p></div><div className="flex gap-2"><button onClick={()=>setShowTest(true)} className="inline-flex items-center gap-2 rounded-lg border border-slate-700 bg-slate-900 px-4 py-2.5 text-sm font-semibold text-slate-200 hover:border-slate-600 hover:bg-slate-800"><ShieldCheck size={16}/> Test SNMP</button><button onClick={()=>setShowDiscovery(v=>!v)} className="inline-flex items-center gap-2 rounded-lg border border-slate-700 bg-slate-900 px-4 py-2.5 text-sm font-semibold text-slate-200 hover:border-slate-600 hover:bg-slate-800"><Search size={16}/> Discover</button><button onClick={()=>setShowAdd(true)} className="inline-flex items-center gap-2 rounded-lg bg-cyan-500 px-4 py-2.5 text-sm font-bold text-slate-950 hover:bg-cyan-400"><Plus size={17}/> Add device</button></div></header>
  {message&&<div className="mb-5 flex items-center justify-between rounded-lg border border-cyan-900/70 bg-cyan-950/30 px-4 py-3 text-sm text-cyan-200"><span>{message}</span><button onClick={()=>setMessage("")}><X size={15}/></button></div>}
  <div className="mb-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={Server} label="Total devices" value={stats.total} hint="Registered infrastructure"/><Stat icon={Activity} label="Monitoring active" value={stats.monitoring} hint={`${stats.total?Math.round(stats.monitoring/stats.total*100):0}% of inventory`} good/><Stat icon={ShieldCheck} label="SNMP configured" value={stats.snmp} hint="Ready for polling" good/><Stat icon={Zap} label="Needs attention" value={stats.attention} hint="SNMP not configured" warn={stats.attention>0}/></div>
  {showDiscovery&&<section className={`${card} mb-6 overflow-hidden`}><div className="flex items-center justify-between border-b border-slate-800 px-5 py-4"><div><h2 className="font-semibold text-white">Subnet discovery</h2><p className="mt-1 text-xs text-slate-500">Find SNMP-responsive routers, switches, OLTs and servers before registering them.</p></div><button onClick={()=>setShowDiscovery(false)} className="text-slate-500 hover:text-white"><X size={18}/></button></div><form onSubmit={startScan} className="grid gap-4 p-5 md:grid-cols-6"><Field label="CIDR" wide><input required name="cidr" placeholder="192.168.88.0/24" className={input}/></Field><Field label="Version"><select name="scanVersion" className={input}><option value="2c">SNMP v2c</option></select></Field><Field label="Community"><input name="scanCommunity" type="password" placeholder="public" className={input}/></Field><Field label="Port"><input name="scanPort" type="number" defaultValue="161" className={input}/></Field><Field label="Timeout"><input name="scanTimeoutMs" type="number" defaultValue="1500" className={input}/></Field><div className="md:col-span-6"><button disabled={scanning} className="inline-flex items-center gap-2 rounded-lg bg-slate-100 px-4 py-2.5 text-sm font-bold text-slate-950 disabled:opacity-50">{scanning?<Loader2 size={16} className="animate-spin"/>:<Search size={16}/>} {scanning?"Starting scan…":"Scan network"}</button></div></form>{scanError&&<div className="mx-5 mb-5 rounded-lg border border-red-900/60 bg-red-950/20 px-4 py-3 text-sm text-red-300">{scanError}</div>}{scanJob&&<div className="border-t border-slate-800 p-5"><div className="mb-3 flex flex-wrap items-center justify-between gap-2 text-xs text-slate-400"><span>{scanJob.cidr} · {scanJob.status==="running"?`Scanning ${scanJob.scanned}/${scanJob.total}`:`Complete · ${scanJob.results.length} responsive host(s)`}</span>{selectedAddrs.size>0&&<button onClick={importSelected} disabled={importing} className="rounded-lg bg-emerald-500 px-3 py-2 text-xs font-bold text-slate-950">{importing?"Importing…":`Import ${selectedAddrs.size} selected`}</button>}</div>{scanJob.status==="running"&&<div className="mb-4 h-1.5 overflow-hidden rounded-full bg-slate-800"><div className="h-full bg-cyan-400 transition-all" style={{width:`${scanJob.total?Math.round(scanJob.scanned/scanJob.total*100):0}%`}}/></div>}<div className="overflow-x-auto"><table className="w-full text-left text-xs"><thead className="text-slate-500"><tr><th className="pb-2 w-8"></th><th>Address</th><th>System</th><th>Type</th><th>Vendor</th></tr></thead><tbody>{scanJob.results.map(f=><tr key={f.address} className="border-t border-slate-800/70"><td className="py-2"><input type="checkbox" checked={selectedAddrs.has(f.address)} onChange={()=>setSelectedAddrs(p=>{const n=new Set(p);n.has(f.address)?n.delete(f.address):n.add(f.address);return n})}/></td><td className="py-2 font-mono">{f.address}</td><td className="py-2">{f.systemName||"—"}</td><td className="py-2 uppercase text-slate-500">{f.deviceType}</td><td className="py-2 text-slate-400">{f.vendor||"—"}</td></tr>)}</tbody></table></div></div>}</section>}
  <section className={card}><div className="flex flex-col gap-3 border-b border-slate-800 px-5 py-4 md:flex-row md:items-center md:justify-between"><div><h2 className="font-semibold text-white">Device inventory</h2><p className="mt-1 text-xs text-slate-500">Select a device to monitor it. Use <b>Add SNMP</b> when credentials are not yet configured.</p></div><div className="flex flex-wrap items-center gap-2">{checked.size>0&&<><span className="text-xs text-slate-500">{checked.size} selected</span><button onClick={()=>bulkPause(false)} disabled={bulkBusy} className="rounded-md border border-slate-700 px-3 py-2 text-xs text-amber-300">Pause</button><button onClick={()=>bulkPause(true)} disabled={bulkBusy} className="rounded-md border border-slate-700 px-3 py-2 text-xs text-emerald-300">Resume</button></>}<button onClick={load} className="inline-flex items-center gap-2 rounded-md border border-slate-700 px-3 py-2 text-xs text-slate-300 hover:bg-slate-800"><RefreshCw size={14}/> Refresh</button></div></div><div className="overflow-x-auto"><table className="w-full min-w-[1050px] text-left text-sm"><thead className="border-b border-slate-800 bg-slate-950/60 text-[11px] uppercase tracking-wider text-slate-500"><tr><th className="w-10 px-5 py-3"></th><th className="px-3 py-3">Device</th><th className="px-3 py-3">Address</th><th className="px-3 py-3">Role</th><th className="px-3 py-3">Vendor</th><th className="px-3 py-3">SNMP</th><th className="px-3 py-3">Monitor</th><th className="px-3 py-3">Group</th><th className="px-5 py-3 text-right">Actions</th></tr></thead><tbody>{loading?<tr><td colSpan={9} className="py-14 text-center text-slate-500"><Loader2 className="mx-auto mb-2 animate-spin" size={20}/>Loading inventory…</td></tr>:devices.length?groupsView.map(section=><GroupSectionRows key={section.key} section={section} collapsed={collapsed.has(section.key)} onToggle={()=>toggleCollapsed(section.key)} render={d=><tr key={d.id} className="border-b border-slate-800/60 hover:bg-slate-800/25"><td className="px-5 py-3"><input type="checkbox" checked={checked.has(d.id)} onChange={()=>toggleChecked(d.id)} className="h-4 w-4 rounded border-slate-700 bg-slate-950"/></td><td className="px-3 py-3"><Link href={`/devices/${d.id}`} className="flex items-center gap-3"><DeviceIcon type={d.deviceType}/><span><span className="block font-semibold text-slate-100 hover:text-cyan-400">{d.name}</span><span className="block text-[11px] text-slate-600">{d.serialNumber||"No serial recorded"}</span></span></Link></td><td className="px-3 py-3 font-mono text-xs text-slate-300">{d.address}</td><td className="px-3 py-3 text-xs uppercase text-slate-500">{d.deviceType}</td><td className="px-3 py-3 text-xs text-slate-400">{d.vendor||"—"}</td><td className="px-3 py-3">{d.snmpConfigured?<span className="inline-flex items-center gap-1.5 rounded-full border border-emerald-900/70 bg-emerald-950/30 px-2.5 py-1 text-[10px] font-bold text-emerald-300"><CheckCircle2 size={12}/> v{d.snmpVersion}</span>:<span className="inline-flex rounded-full border border-amber-900/70 bg-amber-950/20 px-2.5 py-1 text-[10px] font-bold text-amber-300">Not configured</span>}</td><td className="px-3 py-3"><span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-bold ${d.enabled?"bg-emerald-950/30 text-emerald-300":"bg-slate-800 text-slate-500"}`}>{d.enabled?"ACTIVE":"PAUSED"}</span></td><td className="px-3 py-3"><select aria-label={`Group for ${d.name}`} value={memberOf[d.id]??""} onChange={e=>assignGroup(d,e.target.value?Number(e.target.value):null)} className="rounded-md border border-slate-700 bg-slate-950 px-2 py-1.5 text-[11px] text-slate-300"><option value="">Ungrouped</option>{groups.map(g=><option key={g.id} value={g.id}>{g.name}</option>)}</select></td><td className="px-5 py-3"><div className="flex justify-end gap-1.5"><Link href={`/devices/${d.id}`} className="rounded-md bg-cyan-500 px-3 py-1.5 text-[11px] font-bold text-slate-950 hover:bg-cyan-400">Open</Link><button onClick={()=>setSelected(d)} className="rounded-md border border-slate-700 px-3 py-1.5 text-[11px] font-semibold text-slate-200 hover:bg-slate-800">{d.snmpConfigured?"SNMP":"Add SNMP"}</button><button onClick={()=>cloneDevice(d)} title="Clone device" className="rounded-md border border-slate-700 p-1.5 text-slate-500 hover:text-slate-200"><Copy size={14}/></button><button onClick={()=>togglePause(d)} title={d.enabled?"Pause monitoring":"Resume monitoring"} className="rounded-md border border-slate-700 p-1.5 text-slate-500 hover:text-slate-200">{d.enabled?<Pause size={14}/>:<Play size={14}/>}</button></div></td></tr>}/>):<tr><td colSpan={9} className="py-16 text-center"><div className="mx-auto mb-3 flex h-12 w-12 items-center justify-center rounded-full bg-slate-800"><Server size={22} className="text-slate-500"/></div><div className="font-semibold text-slate-200">No devices registered</div><p className="mt-1 text-xs text-slate-500">Start with Add device or discover your subnet.</p></td></tr>}</tbody></table></div></section>
  <div className="mt-4 flex items-center justify-between text-[11px] text-slate-600"><span>RoutingNMS inventory · SNMP v2c/v3</span><span>Backend-driven · changes are persisted in PostgreSQL</span></div>
  {showAdd&&<Modal title="Add network device" subtitle="Register the device and optionally enable SNMP in the same workflow." onClose={()=>setShowAdd(false)}><form ref={addFormRef} onSubmit={add} className="space-y-5"><Step n="01" title="Identity"><div className="grid gap-4 sm:grid-cols-2"><Field label="Device name"><input required name="name" placeholder="Core-Router-01" className={input}/></Field><Field label="IP address"><input required name="address" placeholder="192.168.88.17" className={input}/></Field><Field label="Device type"><select name="deviceType" className={input}><option value="router">Router</option><option value="switch">Switch</option><option value="olt">OLT</option><option value="server">Server</option><option value="other">Other</option></select></Field><Field label="Vendor"><input name="vendor" placeholder="MikroTik / Huawei / ZTE" className={input}/></Field><Field label="Serial number" wide><input name="serialNumber" placeholder="Optional — used by provisioning workflows" className={input}/></Field></div></Step><Step n="02" title="SNMP access"><div className="grid gap-4 sm:grid-cols-2"><Field label="SNMP version"><select name="version" className={input}><option value="2c">SNMP v2c</option><option value="3">SNMP v3</option></select></Field><Field label="Port"><input name="snmpPort" type="number" defaultValue="161" className={input}/></Field><Field label="Community (v2c)"><input name="community" type="password" placeholder="public" className={input}/></Field><Field label="Username (v3)"><input name="username" placeholder="snmpuser" className={input}/></Field><Field label="Auth protocol"><select name="authProto" className={input}><option value="">None</option><option value="MD5">MD5</option><option value="SHA">SHA</option></select></Field><Field label="Auth password"><input name="authPass" type="password" className={input}/></Field><Field label="Privacy protocol"><select name="privProto" className={input}><option value="">None</option><option value="DES">DES</option><option value="AES">AES</option></select></Field><Field label="Privacy password"><input name="privPass" type="password" className={input}/></Field><Field label="Timeout"><input name="timeoutMs" type="number" defaultValue="3000" className={input}/></Field></div></Step><div className="flex items-center justify-end gap-2 border-t border-slate-800 pt-4"><button type="button" onClick={()=>setShowAdd(false)} className="rounded-lg border border-slate-700 px-4 py-2.5 text-sm text-slate-300">Cancel</button><button disabled={saving} className="inline-flex items-center gap-2 rounded-lg bg-cyan-500 px-5 py-2.5 text-sm font-bold text-slate-950 disabled:opacity-50">{saving&&<Loader2 size={15} className="animate-spin"/>}Register & monitor</button></div></form></Modal>}
  {showTest&&<Modal title="Verify SNMP connectivity" subtitle="Test credentials before adding a device to monitoring." onClose={()=>setShowTest(false)}><form onSubmit={test} className="space-y-4"><div className="grid gap-4 sm:grid-cols-2"><Field label="Device name"><input required name="name" placeholder="Core-Router-01" className={input}/></Field><Field label="IP address"><input required name="address" placeholder="192.168.88.17" className={input}/></Field><Field label="Type"><select name="deviceType" className={input}><option value="router">Router</option><option value="switch">Switch</option><option value="olt">OLT</option></select></Field><Field label="Vendor"><input name="vendor" placeholder="MikroTik" className={input}/></Field><Field label="Version"><select name="version" className={input}><option value="2c">SNMP v2c</option><option value="3">SNMP v3</option></select></Field><Field label="Community"><input required name="community" type="password" placeholder="public" className={input}/></Field><Field label="Port"><input name="snmpPort" type="number" defaultValue="161" className={input}/></Field><Field label="Timeout"><input name="timeoutMs" type="number" defaultValue="3000" className={input}/></Field></div><button disabled={testing} className="inline-flex w-full items-center justify-center gap-2 rounded-lg border border-cyan-800 bg-cyan-950/30 px-4 py-3 text-sm font-bold text-cyan-300">{testing&&<Loader2 size={16} className="animate-spin"/>}{testing?"Testing SNMP…":"Test connection & discover interfaces"}</button>{result&&<div className={`rounded-lg border p-4 ${result.reachable?"border-emerald-900 bg-emerald-950/20":"border-red-900 bg-red-950/20"}`}><div className="flex items-center justify-between gap-3"><b className={result.reachable?"text-emerald-300":"text-red-300"}>{result.reachable?"SNMP reachable":"SNMP test failed"}</b>{result.interfaceCount!==undefined&&<span className="text-xs text-cyan-300">{result.interfaceCount} interfaces</span>}</div>{result.systemName&&<p className="mt-2 text-sm text-slate-300">{result.systemName}</p>}{result.sysDescr&&<p className="mt-1 text-xs text-slate-500">{result.sysDescr}</p>}{result.error&&<pre className="mt-2 whitespace-pre-wrap text-xs text-red-300">{result.error}</pre>}</div>}</form></Modal>}
  {selected&&<Modal title={`SNMP · ${selected.name}`} subtitle={`${selected.address} · ${selected.vendor||selected.deviceType}`} onClose={()=>setSelected(null)}><form onSubmit={saveSNMP} className="space-y-5"><div className="flex items-center justify-between rounded-lg border border-slate-800 bg-slate-950 p-4"><div><b className="text-sm text-white">Enable SNMP monitoring</b><p className="mt-1 text-xs text-slate-500">Poll this device and make interface data available in Monitoring.</p></div><input name="enabled" type="checkbox" defaultChecked={selected.snmpEnabled} className="h-5 w-5"/></div><div className="grid gap-4 sm:grid-cols-2"><Field label="Version"><select name="version" defaultValue={selected.snmpVersion||"2c"} className={input}><option value="2c">SNMP v2c</option><option value="3">SNMP v3</option></select></Field><Field label="Port"><input name="port" type="number" defaultValue={selected.snmpPort||161} className={input}/></Field><Field label="Community"><input name="community" type="password" placeholder="Leave blank to keep current" className={input}/></Field><Field label="Username"><input name="username" placeholder="SNMPv3 username" className={input}/></Field><Field label="Auth protocol"><select name="authProto" className={input}><option value="">None</option><option value="MD5">MD5</option><option value="SHA">SHA</option></select></Field><Field label="Auth password"><input name="authPass" type="password" className={input}/></Field><Field label="Privacy protocol"><select name="privProto" className={input}><option value="">None</option><option value="DES">DES</option><option value="AES">AES</option></select></Field><Field label="Privacy password"><input name="privPass" type="password" className={input}/></Field><Field label="Timeout"><input name="timeoutMs" type="number" defaultValue="3000" className={input}/></Field></div><div className="flex justify-end gap-2 border-t border-slate-800 pt-4"><button type="button" onClick={()=>setSelected(null)} className="rounded-lg border border-slate-700 px-4 py-2.5 text-sm text-slate-300">Cancel</button><button disabled={saving} className="rounded-lg bg-cyan-500 px-5 py-2.5 text-sm font-bold text-slate-950 disabled:opacity-50">{saving?"Saving…":"Save SNMP"}</button></div></form></Modal>}
 </main>
}
function Stat({icon:Icon,label,value,hint,good,warn}:{icon:React.ComponentType<{size?:number}>;label:string;value:number;hint:string;good?:boolean;warn?:boolean}){return <div className={`${card} p-4`}><div className="flex items-start justify-between"><div><div className="text-[11px] uppercase tracking-wider text-slate-500">{label}</div><div className="mt-2 text-2xl font-bold text-white">{value}</div><div className={`mt-1 text-[11px] ${warn?"text-amber-300":good?"text-emerald-300":"text-slate-600"}`}>{hint}</div></div><div className={`rounded-lg p-2 ${warn?"bg-amber-950/30 text-amber-300":good?"bg-emerald-950/30 text-emerald-300":"bg-slate-800 text-slate-400"}`}><Icon size={18}/></div></div></div>}
function DeviceIcon({type}:{type:string}){const Icon=type==="router"?Router:type==="switch"?Network:type==="olt"?Wifi:Server;return <span className="flex h-9 w-9 items-center justify-center rounded-lg bg-slate-800 text-cyan-300"><Icon size={17}/></span>}
function Field({label,children,wide}:{label:string;children:React.ReactNode;wide?:boolean}){return <label className={`block text-xs font-medium text-slate-400 ${wide?"sm:col-span-2":""}`}>{label}{children}</label>}
function Step({n,title,children}:{n:string;title:string;children:React.ReactNode}){return <section className="rounded-lg border border-slate-800 bg-slate-950/40 p-4"><div className="mb-4 flex items-center gap-3"><span className="font-mono text-[10px] text-cyan-400">{n}</span><h3 className="text-sm font-semibold text-white">{title}</h3></div>{children}</section>}
function Modal({title,subtitle,onClose,children}:{title:string;subtitle:string;onClose:()=>void;children:React.ReactNode}){return <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/75 p-4 backdrop-blur-sm"><div className="max-h-[92vh] w-full max-w-2xl overflow-y-auto rounded-2xl border border-slate-700 bg-slate-900 shadow-2xl"><div className="sticky top-0 z-10 flex items-start justify-between border-b border-slate-800 bg-slate-900/95 px-6 py-5 backdrop-blur"><div><h2 className="text-lg font-bold text-white">{title}</h2><p className="mt-1 text-xs text-slate-500">{subtitle}</p></div><button onClick={onClose} className="rounded-md p-1.5 text-slate-500 hover:bg-slate-800 hover:text-white"><X size={18}/></button></div><div className="p-6">{children}</div></div></div>}
